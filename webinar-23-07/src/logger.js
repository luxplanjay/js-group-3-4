export const logUpperCase = msg => {
  console.log('Logger 123 123123message: ' + msg.toUpperCase());
};

export const logLowerCase = msg => {
  console.log('Logger 123 123123message: ' + msg.toLowerCase());
};

const log = msg => {
  console.log('main log ' + msg);
};

export default log;
